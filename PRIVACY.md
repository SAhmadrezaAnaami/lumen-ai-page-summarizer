# Lumen — سیاست حفظ حریم خصوصی

آخرین به‌روزرسانی: ۲۴ سپتامبر ۲۰۲۶

Lumen یک افزونهٔ Chrome برای خلاصه‌سازی صفحهٔ فعال با سرویس‌های هوش مصنوعی سازگار با OpenAI است.

## داده‌های پردازش‌شده

پس از کلیک کاربر روی دکمهٔ خلاصه‌سازی و تأیید رضایت، افزونه عنوان و نشانی صفحه، توضیح متای صفحه (در صورت وجود) و متن قابل‌خواندن صفحه را حداکثر حدود ۱۸٬۰۰۰ نویسه استخراج می‌کند. نام مدل و پیام سیستمی که کاربر وارد می‌کند نیز همراه درخواست ارسال می‌شود.

خلاصهٔ تولیدشده به‌صورت خودکار ذخیره نمی‌شود و فقط در حافظهٔ موقت پنجرهٔ افزونه نمایش داده می‌شود. صفحات داخلی Chrome مانند `chrome://` و Chrome Web Store قابل خواندن نیستند.

## API و کلید دسترسی

نشانی سرویس API و کلید API در تنظیمات افزونه وارد می‌شوند. کلید در `chrome.storage.local` همان مرورگر ذخیره می‌شود و در کد یا بستهٔ افزونه قرار ندارد. هنگام خلاصه‌سازی، کلید فقط به سرویس API انتخابی ارسال می‌شود.

برای سرویس‌های آنلاین HTTPS الزامی است. HTTP فقط برای مدل‌های محلی روی `localhost` یا `127.0.0.1` مجاز است. سیاست نگهداری داده توسط ارائه‌دهندهٔ API انتخابی به شرایط استفاده و سیاست حریم خصوصی همان ارائه‌دهنده بستگی دارد.

## اشتراک‌گذاری

Lumen تبلیغ، تحلیل رفتار کاربر یا فروش داده ندارد و سرور واسطی برای دریافت خلاصه‌ها ندارد. دادهٔ صفحه فقط برای اجرای درخواست خلاصه‌سازی به ارائه‌دهندهٔ API انتخابی فرستاده می‌شود.

## دسترسی‌ها

- `activeTab` و `scripting`: فقط پس از اقدام کاربر برای خواندن صفحهٔ فعال
- `storage`: نگهداری تنظیمات، کلید API و وضعیت رضایت
- دسترسی به میزبان API: فقط پس از درخواست و تأیید کاربر برای میزبان انتخاب‌شده؛ در صورت لغو رضایت، این دسترسی حذف می‌شود

## حذف داده‌ها

برای حذف تنظیمات و کلید ذخیره‌شده، افزونه را حذف کنید یا داده‌های ذخیره‌شوندهٔ آن را از تنظیمات Chrome پاک کنید. حذف دادهٔ دریافت‌شده توسط ارائه‌دهندهٔ API باید مستقیماً با همان ارائه‌دهنده انجام شود.

## تماس

برای پرسش دربارهٔ این سیاست، از راه ارتباطی درج‌شده در صفحهٔ افزونه در Chrome Web Store استفاده کنید.

نشانی پشتیبانی: [نشانی ایمیل یا URL پشتیبانی خود را اینجا قرار دهید]

## انتشار عمومی

پیش از انتشار در Chrome Web Store، همین سیاست را در یک URL عمومی HTTPS (مثلاً GitHub Pages) منتشر کنید و URL عمومی را در فرم حریم خصوصی فروشگاه ثبت کنید.
